# 16 Sep 2026 — Week 6 release

Owner authorized deployment fixes and a local lecture HTML-to-PDF tool. Instructor
approval remains pending today's presentation. See the detailed evidence and
limitations in `../03-build/20260916-week6.md` and operations in `../../ops/README.md`.

## audit-backlog — 2026-09-16

PASS 3 checks
FAIL 1 check

- Coverage: the existing Must functional/legal IDs retain backlog references;
  LR7 is covered by the gate-process research row B16. No validated requirement
  was renumbered or silently added. CR1 remains a separate owner change request.
- Provenance: all 27 product rows retain existing F/NFR/LR and P references.
  P1–P12 remain in the spec; no interview, pain, participant or approval invented.
- Priority: declared functional/legal priorities remain unchanged and agree with
  their corresponding product rows. NFR-only rows retain their existing priority.
- Legal coverage: the eight declared LR mappings remain present, but the audit's
  stronger rule-level check exposes pre-existing gaps beyond that summary table.

### Gaps

- [legal] `rule.md` PDPA rule 22 requires a human-approved guardian-consent plan
  if offered to under-20s; the current LR table has no numbered requirement for
  that condition. ETA rule 10 also requires a verifiable identifier on relied-on
  exports, without a corresponding numbered LR. These are existing course-rule
  gaps, not new features supplied by CR1.

### Suggested fixes

- The human owner should review whether those course rules apply to this on-device
  tool, then explicitly retain/map them into numbered requirements or amend the
  rule with an approved rationale. Do not invent a consent plan or a legal exemption.
  The historical W2 submission remains untouched.

### Completed engineering changes

HTML converter, offline budget, release identity and atomic deployment, CI test gate,
redaction output regression, result-reference cleanup, privacy-copy corrections,
custom domain/TLS and same-origin recording permissions. 26 local tests pass,
including two local-only lecture reference checks (24 self-contained CI tests).

### Still open

B17 approval; B11 complete all-tool processing/network test; B33 full performance
profile; B34 physical reference device; B35 physical handset matrix; Q5/Q6 course
scope decision; authenticated Projects/MCP evidence. See the Week 6 record for
what was actually measured. Route smoke tests are not completion tests.
