#!/usr/bin/env bash
# Production ownership moved to the independent product repository in v0.3.0.
set -euo pipefail
printf '%s\n' 'Production deployment lives in Thiha-Lynn/unilab-releases: app/deploy.sh.' 'This assignment repository publishes only its separate GitHub Pages preview.' >&2
exit 1
